```ts title="app/actions/story.ts" {5}
'use server'

import { b } from "@/baml_client";

// Custom Server Action
export async function writeMeAStoryAction(prompt: string) {
  // You can add any server logic here, like auth, validation, rate limiting, etc.
  const auth = await auth();

  if (!auth) {
    throw new Error("Unauthorized");
  }

  return b.stream.WriteMeAStory(prompt).toStreamable();
}
```